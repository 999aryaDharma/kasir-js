import POSView from "./PosView";

// Halaman ini sekarang menjadi Server Component yang sangat sederhana.
// Tugasnya hanya untuk memuat komponen klien yang berisi semua logika.
export default function POSPage() {
	return <POSView />;
}
